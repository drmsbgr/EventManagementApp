<div class="p-5 mb-4 bg-light rounded-3 shadow-sm">
  <div class="container-fluid py-5">
    <h1 class="display-5 fw-bold">Evento'ya Hoş Geldiniz!</h1>
    <p class="col-md-8 fs-4">
      Türkiye'deki en popüler etkinlikleri keşfedin, kayıt olun ve yönetimini
      kolayca yapın.
    </p>
    <a href="/events" class="btn btn-primary btn-lg" type="button"
      >Tüm Etkinlikleri Gör</a
    >
  </div>
</div>

<h2 class="mb-4 fw-bold text-dark">🔥 En Popüler Etkinlikler</h2>

<% if (featuredEvents && featuredEvents.length > 0) { %>

<div
  id="featuredEventsCarousel"
  class="carousel slide carousel-dark"
  data-bs-ride="carousel"
  data-bs-interval="5000"
>
  <div class="carousel-indicators">
    <% featuredEvents.forEach((event, index) => { %>
    <button
      type="button"
      data-bs-target="#featuredEventsCarousel"
      data-bs-slide-to="<%= index %>"
      class="<%= index === 0 ? 'active' : '' %>"
      aria-current="<%= index === 0 ? 'true' : 'false' %>"
      aria-label="Slide <%= index + 1 %>"
    ></button>
    <% }); %>
  </div>

  <div class="carousel-inner pb-5">
    <% featuredEvents.forEach((event, index) => { %>

    <div class="carousel-item <%= index === 0 ? 'active' : '' %>">
      <div class="row justify-content-center">
        <div class="col-12 col-md-8 col-lg-6">
          <div class="card shadow-lg border-danger h-100 transition-hover">
            <img
              src="<%= event.imagePath || '/static/img/default-event.png' %>"
              class="card-img-top"
              alt="<%= event.title %> Görseli"
              style="height: 300px; object-fit: cover"
            />

            <span
              class="badge bg-danger text-white position-absolute top-0 end-0 m-2 rounded-pill"
              style="margin-top: 10px !important; margin-right: 10px !important"
            >
              Görüntülenme (<%= event.numOfViews %>)
            </span>

            <div class="card-body text-center d-flex flex-column">
              <h5 class="card-title fw-bold text-dark mb-3">
                <%= event.title %>
              </h5>

              <p class="text-muted small mb-3">
                <%= event.desc ? event.desc.substring(0, 150) + '...' : '' %>
              </p>

              <a
                href="/events/<%= event.slug %>"
                class="btn btn-primary mt-auto mx-auto"
              >
                <i class="fas fa-info-circle me-1"></i> İncele
              </a>
            </div>
          </div>
        </div>
      </div>
    </div>

    <% }); %>
  </div>
  <button
    class="carousel-control-prev"
    type="button"
    data-bs-target="#featuredEventsCarousel"
    data-bs-slide="prev"
  >
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="visually-hidden">Previous</span>
  </button>
  <button
    class="carousel-control-next"
    type="button"
    data-bs-target="#featuredEventsCarousel"
    data-bs-slide="next"
  >
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="visually-hidden">Next</span>
  </button>
</div>
<hr class="my-5" />

<% } %>
