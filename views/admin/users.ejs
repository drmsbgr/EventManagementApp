<div class="d-flex justify-content-between align-items-center mb-4">
    <h1 class="display-6 fw-bold"><i class="fas fa-users-cog me-2 text-primary"></i> Kullanıcı Yönetimi</h1>
</div>
<p class="lead text-muted">Sisteme kayıtlı tüm kullanıcıları listeleyin ve yetki rollerini düzenleyin.</p>

<hr>

<% if (typeof error !== 'undefined' && error) { %>
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <i class="fas fa-exclamation-triangle me-2"></i>
        <strong>Hata!</strong> <%= error %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Kapat"></button>
    </div>
<% } %>

<% if (typeof success !== 'undefined' && success) { %>
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        <i class="fas fa-check-circle me-2"></i>
        <strong>Başarılı!</strong> <%= success %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Kapat"></button>
    </div>
<% } %>

<div class="card shadow-lg mb-5">
    <div class="card-body p-0">
        
        <div class="table-responsive">
            <table class="table table-hover table-striped mb-0">
                <thead class="bg-light text-muted">
                    <tr>
                        <th scope="col" style="width: 5%;">#ID</th>
                        <th scope="col" style="width: 25%;">Kullanıcı Adı</th>
                        <th scope="col" style="width: 30%;">E-posta</th>
                        <th scope="col" style="width: 20%;">Rol (Güncel)</th>
                        <th scope="col" style="width: 20%;">İşlemler</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (users && users.length > 0) { %>
                        <% users.forEach(user => { %>
                            <tr>
                                <th scope="row"><%= user.id %></th>
                                <td><%= user.username %></td>
                                <td><%= user.email %></td>
                                <td>
                                    <% if (user.role === 'admin') { %>
                                        <span class="badge bg-danger">ADMIN</span>
                                    <% } else { %>
                                        <span class="badge bg-success">USER</span>
                                    <% } %>
                                </td>
                                <td>
                                    <form action="/admin/users/update-role" method="POST" class="d-flex align-items-center">
                                        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                                        <input type="hidden" name="userId" value="<%= user.id %>">
                                        
                                        <select name="newRole" class="form-select form-select-sm me-2" required 
                                            <% if (user.id === locals.user.id) { %>disabled<% } %>>
                                            
                                            <option value="user" <%= user.role === 'user' ? 'selected' : '' %>>USER</option>
                                            <option value="admin" <%= user.role === 'admin' ? 'selected' : '' %>>ADMIN</option>
                                        </select>
                                        
                                        <button type="submit" class="btn btn-sm btn-primary flex-shrink-0" title="Rolü Güncelle" 
                                            <% if (user.id === locals.user.id) { %>disabled<% } %>>
                                            <i class="fas fa-save"></i>
                                        </button>
                                        
                                        <button type="button" class="btn btn-sm btn-danger ms-1" 
                                            title="Kullanıcıyı Sil" 
                                            data-bs-toggle="modal" 
                                            data-bs-target="#deleteUserModal<%= user.id %>" 
                                            <% if (user.id === locals.user.id) { %>disabled<% } %>>
                                            <i class="fas fa-trash-alt"></i>
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        <% }); %>
                    <% } else { %>
                        <tr>
                            <td colspan="5" class="text-center text-muted py-4">
                                Sistemde kayıtlı kullanıcı bulunmamaktadır.
                            </td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        </div>
        
    </div>
</div>

<% if (users && users.length > 0) { %>
    <% users.forEach(user => { %>
        <div class="modal fade" id="deleteUserModal<%= user.id %>" tabindex="-1" aria-labelledby="deleteUserModalLabel" aria-hidden="true">
            <div class="modal-dialog">
                <div class="modal-content">
                    <div class="modal-header bg-danger text-white">
                        <h5 class="modal-title" id="deleteUserModalLabel">Kullanıcı Silme Onayı</h5>
                        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
                    </div>
                    <div class="modal-body">
                        <strong><%= user.username %></strong> adlı kullanıcıyı kalıcı olarak silmek istediğinizden emin misiniz? Bu işlem geri alınamaz ve kullanıcının tüm kayıtları silinecektir!
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">İptal</button>
                        <form action="/admin/users/delete" method="POST" class="d-inline">
                            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                            <input type="hidden" name="userId" value="<%= user.id %>">
                            <button type="submit" class="btn btn-danger">Evet, Sil</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    <% }); %>
<% } %>