<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <a class="navbar-brand" href="/"
      ><img
        src="/static/favicon.png"
        class="img-fluid m-1"
        alt="Sayfa ikonu"
        width="50"
      />
      Evento</a
    >
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
      aria-controls="navbarNav"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link" href="/events">Etkinlikler</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/faq">SSS</a>
        </li>
        <li class="nav-item">
          <a class="nav-link text-warning" href="/admin">Yönetim Paneli</a>
        </li>
      </ul>

      <div class="d-flex align-items-center">
        <%- include('./online_counter') %> <% if (isAuthenticated) { %>
        <div class="dropdown">
          <button
            class="btn btn-outline-light dropdown-toggle me-2"
            type="button"
            data-bs-toggle="dropdown"
            aria-expanded="false"
          >
            <i class="fas fa-user-circle me-1"></i> Merhaba, <%= user.username
            %>!
          </button>
          <ul class="dropdown-menu dropdown-menu-end">
            <li><a class="dropdown-item" href="/user/profile">Profilim</a></li>
            <li>
              <a class="dropdown-item" href="/user/registrations">Kayıtlarım</a>
            </li>
            <li><hr class="dropdown-divider" /></li>
            <li>
              <form action="/auth/logout" method="POST" class="m-0">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
                <button type="submit" class="dropdown-item text-danger">
                  <i class="fas fa-sign-out-alt me-1"></i> Çıkış Yap
                </button>
              </form>
            </li>
          </ul>
        </div>
        <% } else { %>
        <a href="/auth/login" class="btn btn-outline-light me-2">Giriş Yap</a>
        <a href="/auth/register" class="btn btn-primary">Kayıt Ol</a>
        <% } %>
      </div>
    </div>
  </div>
</nav>
<% if (latestAnnouncement) { %>
<div class="container-fluid bg-info text-white text-center py-2 shadow-sm">
  <i class="fas fa-bullhorn me-2"></i>
  <span class="fw-bold"><%= latestAnnouncement.title %>:</span>
  <%= latestAnnouncement.content %>
</div>
<% } %>
